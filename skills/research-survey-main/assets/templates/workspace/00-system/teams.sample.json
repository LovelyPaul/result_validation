{
  "_doc": "team-compare 팀 정의 샘플 — 이 파일을 워크스페이스 00-system/teams.json 으로 복사 후 team_compare.py 로 실행. ★실측 가용 조합만 담았다(이 머신에서 Get-Command 확인: codex·claude 가용). 미설치 CLI는 아래 _examples 주석에 예시만 두고 teams 배열엔 넣지 않는다 — 없는 CLI를 넣으면 team_compare가 '미설치' SystemExit로 막는다. producer/reviewer는 교차 벤더가 핵심(같은 벤더끼리는 단일 벤더 미탐을 못 잡는다).",
  "teams": [
    {
      "team_id": "A-codex-claude",
      "producer": {"cli": "codex", "cmd_template": "exec --skip-git-repo-check -m {model}", "model": "gpt-5.6-sol"},
      "reviewer": {"cli": "claude", "cmd_template": "-p --model {model}", "model": "claude-fable-5"}
    },
    {
      "team_id": "B-claude-codex",
      "producer": {"cli": "claude", "cmd_template": "-p --model {model}", "model": "claude-fable-5"},
      "reviewer": {"cli": "codex", "cmd_template": "exec --skip-git-repo-check -m {model}", "model": "gpt-5.6-sol"}
    }
  ],
  "_examples_추가_CLI_참고용": {
    "_note": "이 머신 실측(Get-Command): gemini·ollama·agy도 가용. 아래를 teams 배열의 producer/reviewer로 옮겨 다른 벤더 조합을 구성할 수 있다. 프롬프트는 stdin으로 전달되므로(개행 절단 회피) cmd_template에는 프롬프트 인자를 넣지 않는다 — {model}만 치환된다. 각 CLI의 헤드리스 stdin 프롬프트 수용은 소규모 실행으로 먼저 확인하라(CLI마다 stdin 처리가 다를 수 있음).",
    "gemini": {"cli": "gemini", "cmd_template": "-m {model} -p", "model": "gemini-2.5-pro"},
    "ollama": {"cli": "ollama", "cmd_template": "run {model}", "model": "llama3.1"}
  }
}
